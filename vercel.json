{
  "version": 2,
  "buildCommand": "rm -rf public/assets public/index.html && cp -r frontend/dist/* public/",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "crons": [
    { "path": "/api/telegram/poll-source", "schedule": "0 0 * * *" }
  ]
}
